{
  "name": "pi-jev-router",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Jev-powered model and logical-session router for Pi",
  "license": "MIT",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "tsx --test test/**/*.test.ts",
    "check": "pnpm typecheck && pnpm test",
    "evaluate:live": "tsx scripts/evaluate-live.ts"
  },
  "dependencies": {
    "@typesafe-ai/sdk": "^0.6.0",
    "dotenv": "^17.2.2"
  },
  "devDependencies": {
    "@earendil-works/pi-agent-core": "0.85.1",
    "@earendil-works/pi-ai": "0.85.1",
    "@earendil-works/pi-coding-agent": "0.85.1",
    "@earendil-works/pi-tui": "0.85.1",
    "@types/node": "^24.5.2",
    "tsx": "^4.20.5",
    "typebox": "^1.0.0",
    "typescript": "^5.9.2"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": ">=0.85.1"
  },
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  }
}
